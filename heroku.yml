setup:
  addons:
  - plan: heroku-postgresql
build:
  docker:
    web: Dockerfile
release:
  image: web
  command:
    - python manage.py migrate
run:
  web: gunicorn aa_project.wsgi --workers 2 --log-file -
